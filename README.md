Hassana Abdullahi

n10000326

Summary:
This lab tasks showed how SQL Server triggers can enforce business rules by improving automation, data integrity, and auditing both data and schema changes.
AFTER triggers log successful data modifications, INSTEAD OF triggers replace operations with custom business logic, and DDL triggers monitor schema changes. Recursive triggers showed how repeated self-execution can occur.
